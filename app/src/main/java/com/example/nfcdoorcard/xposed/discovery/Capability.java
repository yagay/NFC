package com.example.nfcdoorcard.xposed.discovery;

/** Stable capabilities used by the hook framework instead of vendor-specific class names. */
public enum Capability {
    RF_CONFIG_WRITE,
    RF_REFRESH_TRIGGER
}
